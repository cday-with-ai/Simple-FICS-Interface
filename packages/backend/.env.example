# Server Configuration
PORT=3001
NODE_ENV=development

# Logging
LOG_LEVEL=info

# Database (optional - will use default if not specified)
# DB_PATH=/path/to/custom/database.db

# FICS Configuration (for future use when we add authentication)
# FICS_USERNAME=your_username
# FICS_PASSWORD=your_password

# API Security (for future use)
# API_KEY=your-secret-api-key